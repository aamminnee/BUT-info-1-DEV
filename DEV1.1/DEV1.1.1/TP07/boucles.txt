1)
#include<stdio.h>
#include<stdlib.h>

int main(void) {
    int rep1, rep2;
    int i = 0;
    printf("entrer un entier: \n");
    scanf("%d", &rep1);
    printf("entrer un entier: \n");
    scanf("%d", &rep2);
    if (rep1 < rep2) {
        for(rep1; rep1 < rep2; rep1++) {
            printf("%d \n",rep1);
        }
    } else {
        for(rep2; rep2 < rep1; rep2++) {
            printf("%d \n", rep2);
        }
    }
}

2)
#include<stdio.h>
#include<stdlib.h>

int main(void) {
    float rep = 1001.0;
    int i = 1;
    while(rep >= 1000 && rep <= 50000) {
        printf("enter la somme d'argent non compris entre 1000 et 50000 euros: \n");
        scanf("%f", &rep);
    }
    for (i; i <8; i++) {
        rep *= 1.04;
        printf("vous gagnerez %.2f euros, au bout de %d ans. \n", rep, i);
    }
}

3)
#include<stdio.h>
#include<stdlib.h>

int main(void) {
    int rep;
    int i = 0;
    int temp;
    printf("enter un entier: \n");
    scanf("%d", &rep);
    temp = rep;
    for (i; i < 11; i++) {
        rep = temp * i;
        printf("%d * %d = %d\n", temp, i, rep);
    }
}

4)
#include<stdio.h>
#include<stdlib.h>

int main(void) {
    int rep;
    int i;
    int temp;
    while(rep != -1) {
        printf("enter un entier: \n");
        scanf("%d", &rep);
        temp = rep;
        if (rep != -1) {
            for (i = 0; i < 11; i++) {
                rep = temp * i;
                printf("%d * %d = %d\n", temp, i, rep);
            }
        }
    }
    return EXIT_SUCCESS;
}

5)
#include<stdio.h>
#include<stdlib.h>

int main(void) {
    int rep;
    int max = 0;
    while(rep != -1) {
        printf("enter un entier: \n");
        scanf("%d", &rep);
        max = (rep >= max) ? rep : max;
    }
    printf("%d \n", max);
    return EXIT_SUCCESS;
}
