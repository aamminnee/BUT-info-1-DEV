1)
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    char rep = 77;
    printf("%d \n", (int)rep);
    printf("%d \n", (unsigned int)rep);
    printf("%u \n", (unsigned char)rep);
    printf("%d \n", (int)rep);
    printf("%hd \n", (short int)rep);
    printf("%d \n", (unsigned int)rep);
    printf("%hu \n", (unsigned short int)rep);
    printf("%d \n", (int)rep);
    printf("%u \n", (unsigned int)rep);
    printf("%ld \n", (long int)rep);
    printf("%lu \n", (unsigned long int)rep);
    return EXIT_SUCCESS;
}

2)
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int rep;
    printf("entre la valeur de l'exo : \n");
    scanf("%d", &rep);
    printf("%d \n", rep);
    rep = (char)rep;
    printf("%hd \n", rep);
    return EXIT_SUCCESS;
}

3)
#include <stdio.h>
#include <stdlib.h>

int main(void) {
  int res = 0;
  int i;
  for(i = 1; i <= 7; i++) {
    int rep;
    printf("nombre de visiteur au jours %d: \n", i);
    scanf("%d", &rep);
    res += rep;
  }
  res = (res / 7);
  printf("%d", res);
  return EXIT_SUCCESS;
}

4)
#include <stdio.h>
#include <stdlib.h>

int main(void) {
  float produit = 5.49;
  float rendu = 0.0f;
  float clients;
  printf("le client a payé : \n");
  scanf("%f", &clients);
  clients -= produit;
  while(clients > 0.01f) {
    if (clients >= 2.0f) {
      clients -= 2.0f;
      printf("2 \n");
    } else if (clients >= 0.2f) {
      clients -= 0.2f;
      printf("0.2 \n");
    } else {
      clients -= 0.1f;
      printf("0.1 \n");
    }
  }
  return EXIT_SUCCESS;
}


