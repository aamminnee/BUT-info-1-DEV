#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
  int rayon, i, j, space;
  printf("Rayon ? \n");
  scanf("%d", &rayon);
  space =  rayon / 2;
  for(i = 0; i < space; i++) {
    for(j = 0; j < )
  return EXIT_SUCCESS;
}

